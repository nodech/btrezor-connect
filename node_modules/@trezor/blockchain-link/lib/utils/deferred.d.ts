export declare function create<T>(id: number | string): Deferred<T>;
export interface Deferred<T> {
    id: number | string;
    promise: Promise<T>;
    resolve: (t: T) => void;
    reject: (e: Error) => void;
}
